name = "Hugo Redirect"
license = "MIT"
licenselink = "https://github.com/gcc42/hugo-redirect/blob/master/LICENSE.md"
description = "Hugo theme component to add easy redirects in a site"
tags = ["redirect", "hugo", "component"]
features = [
		"generates _redirect files for netlify",
		"Supports static meta refresh based redirects",
		"redirects"
]
min_version = 0.67

[author]
    name = "gcc34"
    homepage = "https://prag.io"
	